<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State pressed -->
    <item android:state_pressed="true" android:drawable="@color/whitebroke" />

    <!-- State default (normal) -->
    <item android:drawable="@color/white" />
</selector>
